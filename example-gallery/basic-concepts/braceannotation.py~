from manim import *



class BraceAnnotation(Scene):
    def construct(self):
        self.camera.background_color = RED
        triangle = Triangle(color=WHITE, fill_opacity=1)
        self.play(triangle.animate.rotate(-PI/2).scale(2))
        self.wait()
